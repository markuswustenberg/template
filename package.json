{
  "name": "template",
  "version": "0.1.0",
  "description": "A template for web projects.",
  "author": "Markus Wüstenberg",
  "license": "MIT",
  "scripts": {
    "build": "yarn run clean && yarn run copy-static && NODE_ENV=production browserify -t [ babelify ] source/app.js | uglifyjs - -m -c -o build/app.js",
    "clean": "mkdir -p build && mkdir -p docs && rm -rf build/* && rm -rf docs/*",
    "copy-static": "cp -a static/* build/",
    "lint": "eslint source tests",
    "publish": "mkdir -p docs && yarn run build && cp -a build/ docs",
    "start": "static build",
    "test": "yarn run lint && browserify -t [ babelify ] tests/app.js | node | faucet",
    "watch": "yarn run clean && yarn run copy-static && watchify -v -d -t [ babelify ] source/app.js -o build/app.js"
  },
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "react-redux": "^4.4.5",
    "redux": "^3.6.0",
    "redux-logger": "^2.7.0",
    "redux-thunk": "^2.1.0"
  },
  "devDependencies": {
    "babel-plugin-transform-object-rest-spread": "^6.16.0",
    "babel-preset-latest": "^6.16.0",
    "babel-preset-react": "^6.16.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "eslint": "^3.8.1",
    "eslint-plugin-react": "^6.4.1",
    "faucet": "^0.0.1",
    "node-static": "^0.7.9",
    "tape": "^4.6.2",
    "uglify-js": "^2.7.3",
    "watchify": "^3.7.0"
  }
}
